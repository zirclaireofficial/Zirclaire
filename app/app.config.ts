// Nuxt UI theme mapping — see app/assets/css/main.css for the color ramps.
export default defineAppConfig({
  ui: {
    colors: {
      primary: 'brand',   // red — the one accent, does the heavy lifting
      success: 'forest',  // deep green — reserved for success states
      neutral: 'stone',   // warm neutral canvas + ink grounding
    },
  },
})
