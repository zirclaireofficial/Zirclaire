# Allow installs when an optional peer dependency lags behind (e.g.
# @vercel/speed-insights lists vue-router@^4 while Nuxt 4 uses v5).
# Committed so Vercel build install matches local.
legacy-peer-deps=true
